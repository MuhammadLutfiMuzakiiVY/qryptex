[package]
name = "qrypt-analysis"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Hardness estimation, QROM reduction bounds, and timing side-channel audit tools for QRYPTEX"

[dependencies]
qrypt-core = { path = "../qrypt-core" }
qrypt-kem = { path = "../qrypt-kem" }
qrypt-signature = { path = "../qrypt-signature" }
qrypt-combiner = { path = "../qrypt-combiner" }
rand_core.workspace = true
rand_chacha.workspace = true
